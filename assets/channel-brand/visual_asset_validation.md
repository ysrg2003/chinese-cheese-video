# Visual asset validation

## Reviewed assets

| Asset | Result | Notes |
| --- | --- | --- |
| `xiangqi-lab-youtube-banner.png` | Pass | Confirmed 2560×1440. The English text reads `XIANGQI LAB` and `THE STRATEGY LAB FOR CHINESE CHESS`; the emblem and all required text sit in the central horizontal band, leaving safe outer margins. The palette is consistent with the brand book. |
| `xiangqi-lab-youtube-banner-zh.png` | Pass | Confirmed 2560×1440. The Chinese text reads `中国象棋实验室` and `看懂战术，下好每一步。`; the emblem and text are placed in the central horizontal band. The palette and geometric board motifs match the English asset. |

## Decision

Both banners are suitable for channel upload. The English banner is the primary upload asset because English is the channel's default public language. The Chinese banner is a companion asset for later localized campaigns, a potential Chinese sister channel, or an updated channel-art cycle.

| Asset | Result | Notes |
| --- | --- | --- |
| `xiangqi-lab-thumbnail-en-1280x720.png` | Pass | Confirmed 1280×720. `ONLY MOVE?` is large, clear, and matched by a single board situation and one tactical arrow. The Xiangqi Lab label is legible. |
| `xiangqi-lab-thumbnail-zh-1280x720.png` | Pass | Confirmed 1280×720. `唯一胜着？` and `中国象棋实验室` are correct and clearly readable. The design preserves the same hierarchy and icon system as the English version. |

The template visuals are suitable as a style benchmark. For production episodes, replace the displayed position and the large promise text so that they correspond exactly to the episode's real board state and solution.
